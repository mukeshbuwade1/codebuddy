import { SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Form from './scr/screens/Form'

const App = () => {
  return (
    <SafeAreaView>
      <Form/>
    </SafeAreaView>
  )
}

export default App

const styles = StyleSheet.create({})